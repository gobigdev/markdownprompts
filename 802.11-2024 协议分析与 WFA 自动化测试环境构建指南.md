# 802.11-2024 协议分析与 WFA 自动化测试环境构建指南

## 一、 协议分析基础设施与检索策略

为了在后续的协议分析中做到**绝对精准、不造名词、对齐标准**，我们制定了以下基础设施和检索方案：

### 1. 核心资源配合关系
*   **pcap 抓包文件（第一优先级）：** 理论联系实际的唯一桥梁。用于将 802.11-2024 中的抽象定义（如 MLO 建立、SMPS/EMLSR 切换）映射到 Wireshark 的具体 Information Element (IE) 字段和状态机跳转中。
*   **OpenWiFi 源码（按需引入）：** 硬件与固件级参考。用于在分析“驱动/固件如何响应协议”时，审计 C 语言结构体、寄存器配置以及 Low-MAC 硬件时序是否符合标准。

### 2. 1.4MB 巨型 TOC 目录的 Gemini RAG 定位方案
由于 1.4MB 的目录文件过大，直接喂给 LLM 成本高且易失焦，我们采用**两阶段本地加速 RAG 架构**：
*   **第一阶段（本地高速粗筛）：** 运行本地 Python 脚本，利用高效正则表达式（如匹配 `MLO`, `EHT` 等）对 `TOC.md` 进行流式扫描，瞬间将 1.4MB 的文本浓缩到几 KB。
*   **第二阶段（Gemini 精准研判）：** 将过滤后的精简上下文连同您的大白话技术疑问提交给 Gemini（推荐使用 `gemini-1.5-flash`），让其分析并输出精确的 Section 对应文件名（如 `Section_9.4.2.275.md`）。
*   *(注：核心检索脚本 `wifi_protocol_router.py` 已为您本地生成。)*

### 3. 协议 MD 翻译原则
*   **术语绝对留白：** 所有协议专有名词（如 `Element ID`, `Link Identifier`, `SMPS`, `Bit`, `Octet` 等）在翻译时必须严格保留英文原词。
*   **中英双语段落对照：** 翻译后保持原始 Markdown 的标题层级和表格结构，采用中英对照方式，以便于驱动开发语境下的无缝审计。

---

## 二、 WFA 测试环境与 CAPI/Sigma 自动化链条

要验证驱动行为是否完全符合 Wi-Fi Alliance (WFA) 的测试规范，您无需采购动辄数十万甚至上百万的商业测试床（如 octoScope/Keysight），可以直接利用**完全开源的软件生态 + 开源硬件/路由器**进行 1:1 复刻。

### 1. WFA 开源生态核心拼图与软件来源

| 组件名称 | 扮演的角色 | 核心功能与价值 | 官方开源地址 / 软件来源 |
| :--- | :--- | :--- | :--- |
| **CAPI Technical Specification** | **控制协议字典** | 详细定义了所有测试命令（如 `ap_config_commit`, `sta_config_wmm`）的语法、参数与返回值。 | [Wi-Fi Alliance 官网](https://www.wi-fi.org/) 搜索或直接通过 Google 检索 `Wi-Fi Test Suite Control API Technical Specification` 下载公开 PDF 文件。 |
| **Wi-Fi Test Suite UCC** (Unified CAPI Console) | **自动化测试“大脑”** | 运行在测试 PC 上的控制台，读取包含 CAPI 命令序列的 `.txt` 测试脚本，并通过 TCP 向 AP 和 STA 自动下发指令。 | **GitHub 官方开源仓库：** [GitHub - Wi-FiTestSuite-UCC](https://github.com/Wi-FiTestSuite/Wi-FiTestSuite-UCC) |
| **wfa-testsuite** (wfa_dut) | **测试 AP 代理 (Agent)** | 运行在 OpenWrt AP 上。在指定端口监听，将收到的 CAPI 命令自动桥接到 OpenWrt 本地的 `uci`、`hostapd_cli` 或 `iw` 驱动配置。 | **OpenWrt 官方包管理器：** 直接在路由器终端执行 `opkg install wfa-testsuite` 即可自动下载并安装适配好的二进制包。 |
| **Linux CAPI Agent** (wfa_dut 源码) | **测试 STA 代理 (Agent)** | 运行在待测 Station（如 openWiFi 客户端）上。配合 UCC 脚本自动执行关联、打流、省电状态切换等测试动作。 | **GitHub 官方开源仓库：** [GitHub - Wi-FiTestSuite-Linux-DUT](https://github.com/Wi-FiTestSuite/Wi-FiTestSuite-Linux-DUT) <br> *(可克隆此源码编译并移植到您的 openWiFi 客户端中)* |

### 2. 本地 1:1 自建 WFA 自动化测试拓扑关系
*   **测试主机 (PC 端)：** 运行 `Wi-Fi Test Suite UCC` 控制台（从 GitHub 克隆源码至本地运行），通过 TCP/IP 网络向 AP 和 STA 发送标准的 CAPI 控制指令。
*   **OpenWrt 路由器 (AP 端)：** 运行 `wfa-testsuite` 中的 `wfa_dut` 监听服务（直接通过 `opkg` 一键获取）。接收到 PC 端的 CAPI 指令后，自动转化为本地驱动配置，向空口发射符合测试规范的无线信号。
*   **待测客户端 (STA 端)：** 您的 openWiFi 硬件同样运行 `wfa_dut` 代理（基于 GitHub 官方 `Linux-DUT` 源码交叉编译移植），接收 CAPI 指令并执行关联、打流、省电状态切换等测试动作。

### 3. 如何利用该环境闭环分析协议

1.  **查阅用例：** 直接在开源的 `Wi-FiTestSuite-UCC` 仓库中的 `cases/` 或相关测试目录下，查看官方提供的对应特性（如 WMM AC 或省电机制）的 `.txt` 标准测试脚本（Case）。
2.  **一键执行：** 运行 UCC 控制台执行该 Case。UCC 会在微秒级内指挥 OpenWrt AP 和您的 openWiFi 产生完全对齐标准测试规范的空口行为（如动态调整语音队列的 EDCA 参数、发送 DELTS 拆链帧）。
3.  **抓包审计：** 开启混杂模式进行 **pcap 抓包**。
4.  **AI 对照：** 将捕获到的干净、无杂质的特定 Action 帧/关联帧，连同您的本地 TOC 目录交给我。我们可以秒级定位到 802.11-2024 协议中的标准出处，帮助您彻底看懂并吃透底层的每一个 bit 逻辑。
