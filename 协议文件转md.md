我需要你在当前目录下开发并运行一个高效的 802.11be (WiFi 7) 协议 PDF 到 Markdown 的微切分转换工具。这个工具必须使用 Python 和开源的 `docling` 库。

请你扮演资深数据工程师，自主完成以下开发和运行任务（所有需要的 Python 脚本编写、第三方库安装、终端调试都由你独立在后台执行）：

### 1. 核心任务要求
1. **并发批量读取**：使用 Docling 的 `DocumentConverter` 批量扫描当前目录下的 `protocol_pdf/` 文件夹。必须配置适当的线程池（如 ThreadPoolExecutor），以防超大 PDF 阻塞单线程。
2. **文档语义树探查**：遍历 Docling 解析出的文档节点树（Document Nodes），利用节点的 label 和 level 属性捕捉文档的层级结构。
3. **微颗粒度物理切分（关键核心）**：
   - 802.11 协议的单章（Chapter）动辄数百页，太适合大模型检索。因此，请将切片边界下沉到 **二级标题（Level 2，如 35.2）** 或 **三级标题（Level 3，如 35.2.1）**。
   - 一旦遍历到新的 Level 2 或 Level 3 标题，且标题文本符合协议小节编号特征（如 `35.2` 或 `35.2.1` 开头），就立即将前面的内容截断并保存为一个独立的 `.md` 文件。
4. **智能命名与输出**：将切片出的 Markdown 文件统一存放在 `protocol_markdown/` 目录中。文件名必须清晰且极具可读性，格式推荐为：`[原PDF文件名]_[小节编号]_[清洗后的标题名].md`（例如：`80211be_35_2_4_Power_management.md`）。
5. **健壮的异常处理**：如遇某些页面由于密集嵌套表格或复杂公式引发 Docling 模型报错，必须通过 try-catch 捕获异常、记录 Log，并跳过该节点/文件继续向下执行，绝不能中途崩溃退出。

### 2. 你的执行工作流
- **第一步：环境与依赖审计**。检查当前系统 Python 环境，如果未安装 `docling`，请自行在终端执行 `pip install docling`（如需加速请选用合适的国内源）。
- **第二步：编写脚本**。在当前目录下创建一个名为 `convert_spec_micro.py` 的文件，将上述逻辑写进去。
- **第三步：冒烟测试与运行**。在终端实际运行 `python convert_spec_micro.py`。请在脚本中加入进度打印，让我能看到当前正在切分哪个小节。
- **第四步：结果汇报**。转换完成后，检查 `protocol_markdown/` 目录下的文件数量，并向我展示其中任意一个切分好的小节 Markdown 文件结构，以确认表格和标题未发生错位。

你可以现在开始行动，在每一步遇到环境、库版本或代码 Bug 时，请自行在终端调试并修复它，直到最终成功吐出切分好的 markdown 胶囊文件。